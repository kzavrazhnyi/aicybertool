# Cursor Rules для проекту

## 🖥️ Windows-специфічні правила

### Команди та термінал
- ❌ **НЕ використовуємо `&&`** в командах PowerShell
- ❌ **НЕ використовуємо `| cat`** у PowerShell для виводу результатів команд
- ✅ Використовуємо послідовні команди або `;` для розділення
- ✅ Працюємо в Windows PowerShell
- ✅ Для локального запуску сервера: `python -m uvicorn app:app --reload --host 0.0.0.0 --port 8000`
- ✅ Для production тестування: `python start_gunicorn.py`
- ✅ Активація venv: `. venv\Scripts\Activate.ps1` або `venv\Scripts\Activate.ps1`
- ✅ **ЗАВЖДИ перевіряємо поточну директорію** перед запуском скриптів: `pwd` або `Get-Location`
- ✅ **Flutter команди** запускаємо тільки з директорії `aicybertool_flutter/`
- ✅ **Python скрипти** запускаємо з кореня проекту `D:\PYTHON\aicybertool`
- ✅ **Головна папка архітектури** `D:\PYTHON\aicybertool\architecture`
- ✅ **Сервер документації** запускаємо скриптом `scripts\run-docs.ps1 -Serve`
- ✅ **Запуск сервера документації** тільки в активаційному venv
- ❌ **НЕ запускаємо** `mkdocs serve` напряму без активації venv
- ✅ **Єдиний сервер з багатомовністю**: один сервер на `localhost:8080` підтримує всі мови
- ✅ **MkDocs конфігурація** знаходиться в корені проекту `D:\PYTHON\aicybertool\mkdocs-simple.yml`
- ✅ **Запуск сервера документації** тільки з кореня проекту `D:\PYTHON\aicybertool`

### Кодування та символи
- ❌ **НЕ використовуємо Unicode символи** (✅, ❌, 🚀, 📁, тощо) в print() для Windows
- ✅ Використовуємо ASCII символи: "OK:", "ERROR:", "INFO:", "WARNING:"
- ✅ Встановлюємо UTF-8 кодування: `# -*- coding: utf-8 -*-`
- ✅ Використовуємо `encoding="utf-8"` при роботі з файлами

### Віртуальне середовище
- ✅ **Завжди використовуємо `venv`** для ізоляції пакетів
- ✅ Активація: `venv\Scripts\Activate.ps1` або `venv\Scripts\activate`
- ✅ Деактивація: `deactivate`


### Кодування файлів
- ✅ Всі Python файли: UTF-8 без BOM
- ✅ Всі текстові файли: UTF-8
- ✅ Batch файли: Windows-1251 або UTF-8
- ✅ Shell скрипти: UTF-8

### Git коміти
- ✅ **Короткі коміти** - максимум 50 символів в першому рядку
- ✅ Детальний опис в тілі коміту після порожнього рядка
- ✅ Використовуємо префікси: feat:, fix:, docs:, refactor:, test:
- ❌ НЕ використовуємо довгі рядки в commit message
- ❌ **НЕ комітимо автоматично** - тільки за явним запитом користувача
- ✅ **Завжди запитуємо дозвіл** перед виконанням git add, commit, push
- ✅ **Показуємо статус** git status перед комітом

### Git ignore правила
- ✅ **НЕ комітимо** віртуальне середовище `venv/`
- ✅ **НЕ комітимо** бази даних `*.db`, `*.sqlite`
- ✅ **НЕ комітимо** логи `*.log`, `logs/`
- ✅ **НЕ комітимо** тимчасові файли `*.tmp`, `*.temp`
- ✅ **НЕ комітимо** кеш файли `__pycache__/`, `.pytest_cache/`
- ✅ **НЕ комітимо** IDE файли `.vscode/`, `.idea/`
- ✅ **НЕ комітимо** збірки `build/`, `dist/`, `*.egg-info/`
- ✅ **НЕ комітимо** тестові результати `test_results*.txt`
- ✅ **НЕ комітимо** змінні середовища `.env*`
- ✅ **НЕ комітимо** Windows файли `Thumbs.db`, `Desktop.ini`

### Тимчасові файли та папки
- ✅ **Папка `tmp/`** - для тимчасових файлів розробки
- ✅ **Папка `specs/`** - для специфікацій розробки сервісів
- ✅ **Папка `services/`** - для сервісів MVP архітектури
- ✅ **Папка `examples/`** - для прикладів використання
- ✅ **Тимчасові файли** створюємо в `tmp/` або `specs/`
- ✅ **НЕ комітимо** тимчасові файли з `tmp/`
- ✅ **Комітимо** специфікації з `specs/` як частину документації
- ✅ **Комітимо** сервіси з `services/` як основний код
- ✅ **Очищаємо** тимчасові файли перед комітом
- ✅ **Використовуємо** `tmp/` для експериментальних скриптів

### Message Bus та RabbitMQ
- ✅ **Message Bus** - основа для асинхронної комунікації між сервісами
- ✅ **RabbitMQ** - використовуємо для Message Bus
- ✅ **Docker Compose** - для локального запуску RabbitMQ та Redis
- ✅ **Тестування** - завжди тестуємо Message Bus перед комітом
- ❌ **НЕ запускаємо** сервіси без RabbitMQ (крім тестів)


## 🌐 Багатомовна документація

### Підтримка мов
- ✅ **Підтримка двох мов**: українська (за замовчуванням) та англійська
- ✅ **Єдиний сервер**: один сервер на `localhost:8080` з автоматичним перемиканням мов
- ✅ **Структура папок**:
  ```
  docs/docs/
  ├── uk/          # Українська версія (за замовчуванням)
  ├── en/          # Англійська версія
  ├── index.md     # Вибір мови
  └── mkdocs.yml   # Конфігурація з підтримкою i18n
  ```
- ✅ **URL-шляхи**:
  - Українська: `http://localhost:8080/uk/`
  - Англійська: `http://localhost:8080/en/`
  - Вибір мови: `http://localhost:8080/`

### Правила перекладу
- ✅ **Пріоритет мов**: українська → англійська
- ✅ **Обов'язковий переклад**:
  - Заголовки та навігація
  - Опис функціональності
  - Інструкції користувача
- ✅ **Можна залишити англійською**:
  - Код та технічні терміни
  - Назви змінних та функцій
  - API ендпоінти
- ✅ **Навігація між мовами**:
  - Українська: `/uk/` (за замовчуванням)
  - Англійська: `/en/`
  - Перемикання через кнопки в інтерфейсі

### Правила оновлення
- ❌ **НЕ створюємо** файли поза папками `uk/` та `en/`
- ✅ **Оновлення обох мов** при зміні контенту
- ✅ **Синхронізація структури** між мовами
- ✅ **Тестування обох версій** перед комітом

---
*Останнє оновлення: 2025.01.14 - Додано правила для Git комітів та Message Bus* 